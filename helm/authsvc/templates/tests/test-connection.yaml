apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "authsvc.fullname" . }}-test-connection"
  labels:
    {{- include "authsvc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "authsvc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
